package backend;

class CoolUtil
{
    /*
    * Shortcut function to `Std.isOfType(v, t);`
    */
    public static function isOfType(v:Dynamic, t:Dynamic):Bool
    {
        return Std.isOfType(v, t);
    }
}